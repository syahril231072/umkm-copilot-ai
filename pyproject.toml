[project]
name = "umkm-copilot-ai"
version = "0.1.0"
requires-python = ">=3.11"

[tool.mypy]
python_version = "3.12"

show_error_codes = true
pretty = true
warn_unused_configs = true

disable_error_code = ["prop-decorator"]
